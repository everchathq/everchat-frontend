import React from "react";
import styled from "styled-components";

const CaptionText = styled.div`
  font-family: Inter;
  font-style: normal;
  font-weight: normal;
  font-size: 14px;
  line-height: 22px;
  color: rgba(1, 12, 59, 0.4);
`;

export default CaptionText;
